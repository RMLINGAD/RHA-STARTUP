let role;

// Log the initial value of 'role'
console.log(role);

// Call the choices function
choices("NEUROLOGIST");

function choices(specialist) {
  role = specialist;
  return role;
}

// Log the updated value of 'role' after calling choices
console.log(role);
